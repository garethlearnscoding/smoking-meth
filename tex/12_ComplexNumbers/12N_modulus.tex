\documentclass[tikz]{standalone}
\usepackage{xcolor}
\usetikzlibrary{calc}

\ifdefined\exportmode
  % no background
\else
  \pagecolor{black}
  \color{white}
\fi

\tikzset{
    axis/.style={white,->},
    guide/.style={white,dashed},
    point/.style={fill=white,draw=white},
    label/.style={text=white},
    angle/.style={draw=white}
}

\begin{document}

\begin{tikzpicture}[scale=2.5]

  % background (draw FIRST)
  \def\xmin{-0.5}
  \def\xmax{3.5}
  \def\ymin{-0.5}
  \def\ymax{2.3}

  \fill[opacity=0] ($(\xmin-0.5,\ymin-0.5)$) rectangle ($(\xmax+0.5,\ymax+0.5)$);

  % axes
  \draw[axis] (\xmin, 0) -- (\xmax, 0) node[right] {$\mathrm{Re}$};
  \draw[axis] (0,\ymin) -- (0,\ymax) node[above] {$\mathrm{Im}$};

  % Origin
  \node[label,below left] at (0,0) {$O$};

% Points - \draw[point] (z) circle (0.035);

% Dashed guides - 
%\draw[white,dashed] (z) -- (2,0);
%\draw[white,dashed] (0,1.5) -- (z);

% labels - \node[label,POSITION] at (x,y) {$LABEL$};

  % Origin
  \node[label, below left] at (0, 0) {$O$};
  
  % Point z = x + yi
  \coordinate (z) at (2.5, 1.8);
  \draw[point] (z) circle (0.035);

  % Point z = Re(x + yi)
  \coordinate (x) at (2.5,0);
  \draw[point] (x) circle (0.035);
  
  % Dashed line from O to z (modulus r)
  \draw[guide] (0, 0) -- (z);
  
  % Label modulus
  \node[label,above left=0.1cm] at (1.3, 0.9) {$r$};

  % Label 
  
  % Dashed vertical line (imaginary part)
  \draw[guide] (2.5, 1.8) -- (2.5, 0);
  
  % Dashed horizontal line (real part)
  % \draw[dashed] (0, 1.8) -- (2.5, 1.8);
  
  % Labels for x and y
  \node[label,below] at (1.25, 0) {$x$};
  \node[label,right=0.2cm] at (2.5, 0.9) {$y$};
  \node[label,right=0.15cm] at (2.5, 1.8) {$z = x + yi$};
  
  % Right angle indicator
  \draw[angle,thick] (2.35, 0) -- (2.35, 0.15) -- (2.5, 0.15);
  
  % Angle θ at origin
  \draw[angle] (0.6, 0) arc(0:35:0.6);
  \node[label,right=0.1cm] at (0.55, 0.18) {$\theta$};

\end{tikzpicture}

\end{document}